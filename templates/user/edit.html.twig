{% extends 'base.html.twig' %}

{% block header_title %}<h1>Modifier <strong>{{ user.username }}</strong></h1>{% endblock %}
{% block header_img %}{% endblock %}

{% block body %}
    <div class="users-edit-sections margin-top-50">
        <div class="wrapper wrapper-small">
    {#    <div class="row">#}
            {{ form_start(form, {'action' : path('user_edit', {'id' : user.id} )}) }}
{#                {{ form_widget(form) }}#}
{#                {{ dump(user) }}#}
                {{ form_row(form.username) }}
                {{ form_row(form.email) }}
                {{ form_row(form.password) }}
                {{ form_row(form.roles) }}

                <button type="submit" class="button-primary button-blue button-big btn btn-success">Modifier</button>
            {{ form_end(form) }}
    {#    </div>#}
        </div>
    </div>
{% endblock %}
